import sys

TOKEN = sys.argv[1]

'''
question = {
    'name': name,
    'URL':  URL,
    'io': {
         'in': inputs,
         'out': outputs
    }
'''

"""
question = get_question(TOKEN)

put_tcs(question['io'])

make_template(question['name'])
"""
